# Acacia Roofing — Stage History

Architectural decisions and stage-completion records moved out of CLAUDE.md to keep the primary-context file lean. Read this when extending or revisiting a completed stage. CLAUDE.md retains short stubs that link back to these sections.

---

## Stage 3 Brain Coverage Workflow — Rejected (2026-05-06)

Built `proposeCoverage_` / `applyCoverage_` / `dispatchCoverageTags_` + Brain prompt addendum across ten local commits. Five rounds of prompt iteration could not get Brain to reliably emit `PROPOSE_COVERAGE` tags — model-level limit confirmed via FILES IDENTICAL deploy verification + byte-level prompt audit. Brain consistently asked for "Job ID" (no such thing in coverage), pre-listed all of a rep's outstanding leads, asked which specific rows to mark, and hallucinated $ amounts from prompt context. Even structural primacy (COVERAGE WORKFLOW moved to top of basePrompt) + MENTAL MODEL OVERRIDE framing + WRONG-vs-RIGHT worked examples + explicit HARD RULES could not override the model's "rep closed a job, deduct money, confirm before writing" prior.

**Real architecture insight:** commission requests are simultaneous with coverage events, generated by office, never skipped, with structured lead lists. Coverage should be a SIDE EFFECT of commission generation, not a separate Brain workflow. NL parsing of financial operations is the wrong shape; structured form input is the right shape.

**Path forward:** the Acacia Admin Portal (see below) — admin tools shell + rep self-service shell, with coverage as a side effect of commission generation. Brain stays as conversational / query layer (read-only, information-seeking). Long-term goal: canonical Job entity with all tools as interfaces over it.

**Reusable handlers from Stage 3** (kept conceptually, will be re-implemented as portal services in Stage A Commit 2 — clean re-implementation, no commit cherry-pick):
- Brain Audit tab + `appendAuditLog_` schema (originally commit `95f8ece`)
- `proposeCoverage_` FIFO logic + active-rep gate + schema-defensive Reps lookup (originally `2b3ee85`)
- `applyCoverage_` pre-write audit pattern + two-tier idempotency (originally `908d4d9`)

The Stage 3 implementation stack itself was hard-reset away from local `main` on 2026-05-07 prior to starting the portal build (no GitHub push had ever happened, so origin/main was unaffected). The `job-submittal` Apps Script project still has the deployed-but-dead Stage 3 code (handlers + dispatcher + COVERAGE WORKFLOW prompt section) — production behavior unchanged because Brain doesn't reliably emit the tags. Stage F or later will clean it up.

---

## Acacia Admin Portal — Stage A (complete)

Architectural successor to the rejected Stage 3 workflow. Standalone Apps Script project at `acacia-portal/`.

- **Project location:** `acacia-portal/`
- **Apps Script project ID:** `1Q9Xzs_Bu6IebSuL6rIKUXdPFgizDP9FYU8MgUYJ4U7hVTAoatMjI15dT`
- **Type:** Standalone (deployed as web app via `clasp deploy`)
- **@1 deployment URL (stable test target):** `https://script.google.com/macros/s/AKfycbwntPbuOm1v5LfKjVqu_koCMVQK0HmEk7L6Q2lW_Swq6toT_WSdF0Iz61mOJMLEVyRa/exec`
- **@4 deployment URL (current — Stage A complete):** `https://script.google.com/macros/s/AKfycbyNw6yjZHZWK4E9r5jV6wNrWjpzohfs375KrurxvHNKKeKCJWVZ-4ZeJoWLRZQCQjQ/exec`
- **Authentication model:** `executeAs: USER_DEPLOYING` + `access: DOMAIN`. Script runs as henry@ (deployer) with full Master Tracker access; visitors authenticate as themselves via `Session.getActiveUser().getEmail()`. Visitors don't need direct sheet access — the portal mediates.
- **Role detection (`getCurrentUser_` in `Code.js`):**
  - `ADMIN_EMAILS` (henry@, kelcie@, joshua@, office@) takes precedence over Reps sheet — admins who also sell still get the admin shell.
  - `REP` resolved via `RepsService.getRepByEmail` against the Master Tracker `Reps` sheet, Active rows only (Status blank or "active", case-insensitive, matching the `syncBadLeadFormReps` convention).
  - Schema-defensive: missing First Name / Last Name / Email / Status columns hard-fail at the service layer (CRITICAL log) → `getCurrentUser_` surfaces as UNAUTHORIZED with schema-error reason. Same drift-safety standard as the Stage 3 reusable handlers.
  - `UNAUTHORIZED` returns a deny page with the specific reason exposed (empty session, not in admin list / Reps sheet, rep marked inactive, schema error).
- **Why a new Apps Script project rather than extending `job-submittal`:** clean separation (job-submittal is sheet-bound to the Master Tracker and tightly coupled to Brain + form intake; the portal is a standalone web app with its own scopes and deployment lifecycle), lighter surface (independent `Code.js` rather than appending to job-submittal's already-205KB monolith), independent deploy (portal versions don't risk the job-submittal form's stability and vice versa).

### File roster (Stage A complete; Stage B additions marked)

- `Code.js` — `doGet` entry point, `getCurrentUser_` role detection, `getNavLinks_`, `include_` template helper, `emailToTitleCase_` fallback
- `Constants.js` — `PORTAL_VERSION`, `SPREADSHEET_ID`, `LEADS_SPREADSHEET_ID`, `ADMIN_EMAILS`, `ADMIN_DISPLAY_NAMES`, sheet-name constants, cache TTLs, `ADD_COST_ENUM`, `COST_TYPE_PREFIXES`, `AUDIT_OPERATIONS`
- `SheetSchema.js` — central column-mapping helper with per-execution memoization; throws on missing required columns
- `ScriptCache.js` — typed wrapper over `CacheService.getScriptCache` with namespacing + JSON serialization
- `BrainAuditService.js` — `appendAuditLog` writes to Brain Audit tab; strict operation enum; pre-write intent log pattern
- `RepsService.js` — Reps sheet reads (`getRepByEmail`, `getActiveReps`); Active-only resolution. Stage B added `getRepTier(email)` (replaces Stage A stub).
- `JobsService.js` — Master Job Tracker reads + `updateJobFields` partial update with pre-write audit + partial-apply tracking
- `CostsService.js` — Rep Job Costs reads + `proposeCoverage` / `applyCoverage` (Stage 3 ports) + `addCost` admin-gated CREATE. Stage B added `listOutstandingByRep`, `getCostsByRowNumbers`, `applyCoverageByRowNumbers`.
- `CommissionRequestsService.js` (Stage B) — Pending Commission Requests sheet state machine (Pending → Processed); self-referential audit pointers.
- `CommissionGeneratorService.js` (Stage B) — `DriveApp.makeCopy` of template + cell population + Drive filing under `Acacia Commissions/<Year>/<RepLastName>/`.
- `CommissionPdfService.js` (Stage B) — `UrlFetchApp` + Drive OAuth PDF export with locked params; deterministic filename versioning; `sendEmailWithPdf` via `GmailApp.sendEmail` with `from: 'office@acaciaroofs.com'` send-as alias.
- `Routes.js` — server-side view handlers (`getMyJobsHtml` etc.) + view-shape helpers (`Routes._slugifyStatus`, `_formatDate`, `_formatCurrency`, `_sortJobs`, `_renderErrorHtml`)
- `Index.html` — main shell (admin / rep / deny branches); `welcome-bar` persistent meta + `#view-pane` SPA mount
- `MyJobsView.html` — HtmlTemplate fragment: rep job table with status badges
- `ClientSpa.html` — sandbox-safe IIFE: nav-click → `google.script.run` → swap `#view-pane`
- `Stylesheet.html` — Acacia palette, sidebar, view-pane states, jobs-table, status-badge palette
- `appsscript.json` — manifest: CT timezone, web app config, OAuth scopes
- `README.md` — local-only doc with web app URL placeholder

### Service Layer Pattern (Commits 2a + 2b)

Two-tier service stack:
- **Foundational services** (Commit 2a): `SheetSchema`, `ScriptCache`, `BrainAuditService`, `RepsService`. No cross-dependencies among themselves except `BrainAuditService` → `SheetSchema`. Provide column-mapping, cache, audit-log, and rep-lookup primitives.
- **Operational services** (Commit 2b): `JobsService`, `CostsService`. Depend on the foundational layer. Expose business-shaped reads/writes for Master Tracker + Rep Job Costs.

Conventions (uniform across all services):
- **Service-as-namespace:** `const ServiceName = { method: function(...) {...}, _privateHelper: function(...) {...} };` Public methods non-underscored; private helpers leading-underscored.
- **Universal response shape:** `{ success: true, ...payload }` on success; `{ success: false, error: 'message' }` on failure. Mutation methods that may partial-apply also return `partialApply: true` with row pointers and an audit-row reference for reconciliation.
- **Schema-defensive:** every read calls `SheetSchema.getColumns(spreadsheetId, sheetName, requiredColumns)`, which throws on missing required columns. Services catch internally and surface as `success: false` at the public boundary — callers never see throws.
- **Pre-write audit pattern:** mutating methods (`updateJobFields`, `applyCoverage`, `addCost`) write the audit row BEFORE touching data rows. Audit-write fail aborts cleanly with no row mutations. Audit-succeed / row-write fail surfaces as `partialApply` with the audit row pointer for manual reconciliation. Direct port from the Stage 3 reusable handler (`908d4d9`).

### View Layer Pattern (Commit 3)

- **Routes.js holds per-view server handlers.** One top-level function per nav slug (e.g. `getMyJobsHtml`) so `google.script.run` can call them directly. Shared helpers live on the `Routes` namespace (`Routes._formatDate`, `_formatCurrency`, `_slugifyStatus`, `_renderErrorHtml`).
- **View-handler convention:** `getCurrentUser_` first → role gate (REP-only / ADMIN-only) → service call → projection to view shape → `HtmlService.createTemplateFromFile + evaluate` → return rendered HTML string.
- **Service / view boundary responsibility:** services (`JobsService._rowToJob`, `CostsService._rowToCost`) return ALL columns from spreadsheet rows. The view layer is responsible for filtering admin-only fields out of rep responses. NEVER pass raw service responses to the client without per-role projection.
- **Template escaping:** all interpolation uses `<?= %>` (HTML-escaped). `<?!= %>` is reserved for trusted `include_()` calls only — never for data fields.
- **Pattern A:** server-rendered HTML fragments returned via `google.script.run` and swapped into `#view-pane.innerHTML`. No client-side templating; no JSON contract between client and server. Trade-off: simpler than Pattern B (JSON + client render), at the cost of larger payloads and round-trip per nav. For an internal tool with low traffic, the simplicity wins.

### SPA Pattern (Commit 3)

- **ClientSpa.html:** IIFE-wrapped `<script>` included once at the end of `Index.html`'s ADMIN/REP branch (NOT the UNAUTHORIZED branch — deny page has no nav).
- **Sandbox-safe constraints:** `getElementsByClassName` (not `querySelectorAll`), `addEventListener` (not inline `onclick=`), string concatenation (not template literals), `escapeHtml()` for any client-side string before `innerHTML`.
- **View-handler routing:** the IIFE knows which server function to call for each nav slug via inline `if/else` (acceptable while the slug count is small; convert to a static `VIEW_HANDLERS` map if it grows past ~6).
- **Welcome-content lifecycle:** `#welcome-content` removed from DOM on first nav click (idempotent guard via `welcomeContent && welcomeContent.parentNode`); `welcome-bar` (the auth/role/version meta strip) is persistent above `#view-pane`.

---

## Commission Builder Workflow — Stage B (complete — 2026-05-09)

Office-side commission processing flow, replacing the prior manual-spreadsheet-template-by-hand path. Two-button workflow: Generate Commission → Generate PDF → Send to Rep & Mark Processed.

**Service stack (Stage B additions):**
- `CommissionRequestsService` — Pending Commission Requests sheet (16-col canonical schema), state machine (Pending → Processed), self-referential audit-row pointers (Commission Audit Row + Coverage Audit Row).
- `CommissionGeneratorService` — `DriveApp.makeCopy` of template → populate cells → save under `Acacia Commissions/<Year>/<RepLastName>/`. Returns file URL or `partialFileId` on populate failure (no auto-cleanup; office reconciles).
- `CommissionPdfService` — `UrlFetchApp` + Drive OAuth PDF export with locked params (letter, portrait, fitw, no gridlines). Filename versioning is deterministic; manually-renamed PDFs ignored so the version counter survives Drive renames. `sendEmailWithPdf` uses `GmailApp.sendEmail` with `from: 'office@acaciaroofs.com'` send-as alias.
- `CostsService` extended: `listOutstandingByRep(email, prefix?)` for the 3-bucket Commission Builder UI, `getCostsByRowNumbers(rows)` for form-payload row resolution, `applyCoverageByRowNumbers(rows, jobAddress, msg)` — direct (token-less) coverage application that bypasses the proposeCoverage/applyCoverage cache flow.
- `RepsService.getRepTier(email)` — replaces Stage A stub. Reads Cumulative Commissions, walks `REP_TIERS`, returns tier + officeFeeRate. Tier is computed from cumulative BEFORE this commission (Reps sheet's current value).

**Request row state machine:**

```
Pending (no Commission File URL)         ← rep just submitted
  ↓ [Generate Commission]
Pending (Commission File URL set)        ← spreadsheet exists; coverage applied
  ↓ [Generate PDF]
Pending (Commission PDF URL also set)    ← PDF exists; ready to send
  ↓ [Send to Rep & Mark Processed]
Processed (Status, Processed Timestamp, Processed By all populated)
```

Regenerating spreadsheet after edits: manually clear the `Commission File URL` cell on the request row → form returns to State A on reload. (In-UI regen button rejected per Commit 5 review — accidental click risk outweighs convenience.)

**Audit chain — 4 rows per commission, all `leadReference: requestId`:**

- `COMMISSION_GENERATED` — Routes level, after generate + URL update
- `COVERAGE_FROM_COMMISSION` — pre-write inside `applyCoverageByRowNumbers` (uses cost rows' Lead References as leadReference)
- `COMMISSION_PDF_GENERATED` — Routes level, after PDF generation + URL update
- `COMMISSION_PROCESSED` — pre-write inside `markProcessed`

**Drive structure:**

```
Acacia Commissions/                                              (1fVfYMdFsZk2L3db4S6emTOgvfPiDy84n)
├── Templates/                                                   ← contains template .gsheet (manually managed)
└── 2026/                                                        ← year folder, auto-created on demand
    └── Pruitt/                                                  ← rep last name folder, auto-created on demand
        ├── Commission - Mark Pruitt - Smith - 2026-04-15        ← .gsheet
        ├── Commission - Mark Pruitt - Smith - 2026-04-15.pdf    ← PDF v1
        └── Commission - Mark Pruitt - Smith - 2026-04-15 (v2).pdf
```

**Reps Cumulative Commissions update — deliberately deferred.** Stage B does NOT update Cumulative Commissions when a commission is processed. Workflow decision (per Commit 4 review): cumulative should reflect *paid* commissions, not generated-but-unpaid. Future "Commission Paid" Master Tracker status trigger will auto-update. Until then, office manually adjusts Reps as needed. See TODO #22.

---

## Archived from CLAUDE.md on 2026-05-13

### Claude Code + clasp for Apps Script sync — adopted 2026-04-29

Replaced manual Apps-Script-editor-only workflow with `clasp pull` / `clasp push` synced through this git repo. Enables Claude Code (CLI in this working directory) to read/edit project source directly and round-trip via clasp. Local source convention: `.js` extensions (clasp converts to `.gs` server-side). See CLAUDE.md → File Extension Convention + Deploy Workflow Conventions for the operational rules that emerged from this.

### ai-lead-helper bug-fix history — 2026-05-01 migration cleanup

Context for why the ai-lead-helper code looks the way it does. Surfaced during the migration from acaciaroofingleads@gmail.com → henry@acaciaroofs.com (fix for cross-org auth/permission issues).

- **Loop poisoning fix.** `checkBadLeadFormSubmissions` was written so that any throw inside the row loop bubbled to the outer try/catch, which silently logged and exited — the offending row never stamped Processed=Yes, so the same row poisoned every 30-min run. 12+ days of bad-lead reports queued up before this was caught (root cause was an empty `LEADS_CONFIG.ADMIN_CC` recipient making `GmailApp.sendEmail` throw `no recipient`). **Fix:** per-row try/catch wraps each iteration; rows that fail stamp `Error: <msg>` (truncated to 80 chars) so a single bad row can't block the whole pass. Skip-check was loosened from `=== 'Yes'` to truthy so Error rows don't infinite-retry. Same guard added to `onNewLeadSubmitted`'s missing-rep-email path.
- **Email flood fix.** When the backlog drained, the no-match path fired one email per unmatched row — 24 emails in 30 seconds tripped Gmail's daily quota (`Service invoked too many times for one day: email`). **Fix:** rows accumulate into `unmatched` and `reversals` arrays during the loop; a single summary email goes out per run via `sendBadLeadSummaryEmail_()`. Per-row sends were removed entirely. The `LEADS_CONFIG.ADMIN_CC` empty-guard is still honored — no recipient configured, no email sent.
- **Match logic fix.** `matchLeadByAddress` had two compounding bugs: (1) it filtered to `status === 'Pending'` before any match attempt, so every Good-aged lead was unreachable (and reps file reports late), and (2) the word tokenizer ran a regex against an already-stripped string so it always produced one giant concatenated "word", making the `bestScore >= 2` threshold structurally unreachable. Together these dropped 24 of 25 reports as no-match. **Fix:** two-pass match — exact normalized match first across all statuses, then street number + ≥1 significant name token, preferring Pending row but falling back to non-Pending. Stopwords were narrowed to **pure street-type abbreviations only** (rd, dr, ct, st, ln, ave, blvd, pl, cir, pkwy, apt, unit, suite, ste), directionals (n/s/e/w), and state (tx/texas). Long forms (drive, road, court, ...) and Austin-area city names (austin, round, rock, cedar, park, leander, pflugerville, georgetown) are **NOT** stopwords — bias is toward more matches because false-miss cost (lost lead credit, real money) >> false-match cost (manual review via summary email).
- **Status reversal flow.** When a previously-Good lead is flipped to Bad, the run pushes to a `reversals` array which gets a dedicated `=== Status Reversals — Manual Action Required ===` section in the summary email, listing customer/address/rep/prev-status/reason. This signals that a Rep Job Cost row was already written to the Master Tracker by `handleGoodLead()` and needs manual reversal — see Manual Action Items at the top of CLAUDE.md for the running list.
- **Bad-already path.** Duplicate bad-lead reports for an already-Bad lead don't re-queue a credit request (avoids double-charging the lead company). Log line includes both the existing `Reported` cell value and the new rep/reason for audit trail.
- **Previous Script ID (deprecated 2026-05-01).** Old project ID `18lNBsw_3Jt6teoI9EBMNh0_hbaMoRrDrP4kJBGxkJA-dss6VkDqZT_pN` — all triggers deleted, project archived as backup at the migration. Source backup retained in this repo at `ai-lead-helper-old/`.

### Stage C — QBO Integration complete (2026-05-11)

Commits 0, 1, 2, 3, 4, 5, and 5 fix-up. Production-ready QuickBooks Online integration powering the Commission Builder's Job Cost panel with live vendor data.

**What shipped:**
- OAuth handshake against the Intuit production app
- Data queries with refund netting, Project Manager (PM) override, and rep-exclusion logic
- Commission Builder auto-fill from live QBO data
- Drilldown tooltips on Job Cost rows
- Manual refresh button (silent re-render; toast deferred per TODO #27)
- `EntityRef.name` (not `VendorRef.name`) as canonical Purchase entity vendor source — Commit 5 fix-up
- `Constants.js` real-vendor tuning: Juggernaut loose substring match, supplies-account pattern, Alex Rippee removed from `KNOWN_SUPPLEMENT_VENDORS` (he's a Public Adjuster — see TODO #29)

Verified end-to-end against real Acacia QBO data. Deployed as acacia-portal **@16**.

**Carryover:**
- TODO #27 — Commit 4.5 UI polish (toast on refresh, loading overlay, mismatch warning prominence) deferred until office surfaces real friction
- TODO #29 — Public Adjuster categorization bucket distinct from supplements (open)
- TODO #25 — `debugQboState` cosmetic readout fixes for `accessTokenExpiresAt` / `refreshTokenExpiresAt` / `scope`

---

## Archived from CLAUDE.md on 2026-05-28

Recap content trimmed from CLAUDE.md to keep it under the 40k performance threshold (ahead of the contract-builder inventory entry). The CLAUDE.md inventory/brittle-spot entries retain compact pointers back to the blocks below.

### ancillary-submittal — Bug history (date-serialization, fixed @4)

date-serialization bug — raw `Date` objects nested in `outstandingBids[i].submittedAt` broke `google.script.run` IFRAME serialization once any row reached `Bid Sent` status (worked in editor context — divergence trap). Fixed in @4 by stringifying via `Utilities.formatDate` in `getOutstandingBidsForRep_`. Defensive `instanceof Date` check protects against manual edits leaving a non-Date in the cell.

### job-submittal — Claim type (2026-05-20 + 2026-05-21)

Adjuster-report submissions for insurance jobs require rep to select claim type (Standard / Public Adjuster / Appraisal); PA and Appraisal prepend a clarifying header to Greg's supplement email (signals final-invoice-only handling). formData.claimType persists at Master Job Tracker col 41 (auto-created on first submit; blank for cash bids). Partial TODO #29 — broader PA-handling-bucket work (KNOWN_PUBLIC_ADJUSTER_VENDORS, separate categorization, commission GP treatment) stays deferred.

### bad-lead-app — Vendor-aware routing (Phase B, 2026-05-21)

Both submit paths write LEADS TRACKER Source value to new `Lead Source` column on Bad Lead Reports (auto-created) and pre-stamp `Processed='Yes'` for non-Email sources, so ai-lead-helper's polling trigger never queues Tony credit for text leads. Email leads unchanged (blank Processed → polling queues credit). Rep path also syncs `reverseRepJobCostByCustomerAddress_` when source ≠ 'email'. Rep reversals use status `'Reversed (Bad Lead)'`; admin reversals use `'Reversed (Office Override)'`. Full detail in Done log (2026-05-21, bad-lead-app @4).

### bad-lead-app — Admin mode (TODO #35, 2026-05-19)

Admin-allowlist (joshua/kelcie/henry/office) routed via `getInitialMode()` to admin view (`getAdminBadLeadCandidates` + `submitAdminBadLeadReport`); reports leads >48hr (window 14 days, henry@ override 45). Submit fires 4 ops:
- **OP4** (pre-write): Brain Audit row `operation: 'REVERSE'` — audit-fail aborts before sheet writes.
- **OP1:** LEADS TRACKER Status → Bad via script write (no `onLeadStatusEdit` fire).
- **OP2:** Bad Lead Reports handoff, `Processed=''` (polling queues Tony credit batch).
- **OP3:** reverse Rep Job Cost row matched by (Rep Email + Customer + Address) via `reverseRepJobCostByCustomerAddress_` with `'Reversed (Office Override)'`; already-covered rows refuse with `Covered By Job` notice.

Admin↔rep view toggle in header. Full detail in Done log (2026-05-19, bad-lead-app @3).

### bad-lead-app / ai-lead-helper Split — full rationale (2026-05-12)

- **Problem:** ai-lead-helper deploys as USER_ACCESSING (so bulk uploader emails route through Josh's acaciaroofingleads@ identity). This means any web app hosted in the project runs as the rep on submit — reps need write access to Master Tracker and LEADS TRACKER for the bad-lead path. Reps can't have that access (sensitive financial data).
- **Solution:** Two separate Apps Script projects with different deployment topologies. Bulk uploader stays in ai-lead-helper (USER_ACCESSING). Bad lead path lives in bad-lead-app (USER_DEPLOYING + DOMAIN — runs as henry@).
- **Trade-off:** Some helpers are duplicated across projects (see "Known Duplicated Functions" section below). Drift risk accepted given small surface area.
- **Coordination:** Cross-project state lives in shared spreadsheets (Bad Lead Reports tab is the credit-email handoff point; LEADS TRACKER is read by bad-lead-app and written by both projects on different paths).

### ai-lead-helper — Lead date clock shift recap (2026-05-21)

48-hour Pending→Good clock anchors on Date of Lead (col F) instead of Email Sent At (col J); bulk uploader parses date from `apptTime`, hard-blocks if missing/unparseable. Fallback to col J as edge guard. Redistribution semantics unchanged (keeps original Date of Lead anchor). Full detail in Done log (2026-05-21, ai-lead-helper @12).

### Brittle spot (resolved) — USER_ACCESSING + DOMAIN blocks personal Gmail (2026-05-12)

USER_ACCESSING + DOMAIN access blocks personal Gmail accounts. Discovered 2026-05-12 morning. Workaround: USER_ACCESSING + ANYONE + app-level allowlist for cross-domain identity routing (Josh's bulk uploader case).

## Archived from CLAUDE.md on 2026-06-15

Full @24–@27 job-cost-calculator prompt-iteration narrative, trimmed from the CLAUDE.md inventory entry. All four additive prompt fixes failed the Kelsey Bonnell smoke test and were rolled back to @23 code (deploy @28). The CLAUDE.md entry retains a compact Bug-history bullet pointing here. See TODO #44 (clean-room rewrite) and TODO #45 (NRD-on-ACV validation).

### job-cost-calculator — Kelsey Bonnell anti-anchoring @24–@27 (all reverted, 2026-06-04)

- **Bug history:** Kelsey Bonnell AR anti-anchoring backport (@24, 2026-06-04) — the Kelsey Bonnell AR (USAA/ALLCAT carrier) failed extraction ~12% across 17 attempts, ALWAYS with the same wrong answer: `rcv_amount` = $22,563.05 = Dwelling ($21,383.07) + Code Upgrade PWI ($1,179.98) only. The model anchored on the two **Dwelling-named** summary blocks and dropped "Summary for Contents" ($59.54) + "Summary for Other Structures" ($3,706.37) — neither has "Dwelling" in its title. Correct `rcv_amount` = $26,328.96 (all four coverage summaries, PWI included). Root cause: the prior 2-sentence RCV directive used a Dwelling-heavy example list (Dwelling appeared twice, bracketing the list; Contents never named) and its only guard ("Do NOT take a single Summary value if multiple exist") didn't catch a two-value Dwelling-family grab. **Fix:** backported final-invoice-calculator's @15 §5 architecture (INVARIANT / PRIMARY / FALLBACK / GUARD) wholesale, replacing the 2-sentence directive. Field name adapted `total_rcv` → `rcv_amount`; final-invoice's `total_acv` sentence dropped (job-cost-calc has no `total_acv` field). final-invoice-calc was 15-for-15 on this same AR pre-backport, making its §5 the gold standard for this AR class. **Note the role reversal vs the @15 Bug history above:** at @15, job-cost-calc's §5 was the gold standard that final-invoice mirrored; final-invoice then hardened it into PRIMARY/FALLBACK/GUARD, which is now backported here. No anti-Dwelling-anchoring / Contents-aware additions were made — clean architecture port only; if a Contents-blind-spot failure emerges later, patch BOTH projects as a separate iteration.
  - **@25 follow-up (2026-06-04) — @24 backport was INSUFFICIENT.** Kelsey Bonnell still produced the identical $22,563.05 wrong answer twice in incognito on the @24 deploy. The §5 directive that works verbatim in final-invoice-calc does NOT work embedded in job-cost-calc's surrounding prompt — anchoring is preserved by something OUTSIDE §5. @25 is a **hypothesis test, not a confirmed fix**: the example JSON seeded `"rcv_amount": 0` (placeholder) vs final-invoice's concrete `"total_rcv": 35472.50`; @25 mirrors final-invoice's concrete seed (deliberately NOT 26329, the Kelsey answer, which would contaminate the test via seed-echo). Caveat: weaker anchor than final-invoice's (one concrete field amid measurement zeros vs final-invoice's multiple concrete billing fields) — a negative result rules out only the "one concrete anchor amid zeros" variant, not the example-anchor hypothesis broadly. **If @25 also fails, next step is deterministic pre-processing: extract `rcv_amount` by regex over the coverage-summary blocks, not the LLM.** Awaiting smoke test.
  - **@26 (2026-06-04) — @24 + @25 both DISPROVEN; deeper diagnostic → two targeted edits.** @25's concrete-seed test failed too (Kelsey still $22,563.05 in incognito). A full prompt-context diagnostic compared this prompt against final-invoice-calc's (15/15 on the same AR with the **identical** §5) and ranked two highest-likelihood discriminators (both 5/5): **D1** — final-invoice ends with a closing recency guard ("double-check total_rcv = sum of all section RCVs") that this prompt lacked; **D2** — autoregressive field order: final-invoice emits `total_rcv` *after* depreciation/PWI fields that force a multi-section scan, whereas this prompt emitted `rcv_amount` 4th (cold), before any section work, amid measurement fields. (Lower-ranked: missing §4 depreciation "scan anywhere" rehearsal, MULTI-STRUCTURE roofable-framing bias.) @26 tests **D1+D2 together**: (1) closing recency guard added as the final prompt line, naming Dwelling / Other Structures / Contents explicitly; (2) `rcv_amount` moved to the END of the example JSON (after `line_items`) so the model totals over sections it has already enumerated; @25's 35472.50 seed reverted to 0. **This is the third and final prompt-fix attempt — bounded experiment.** If @26 also fails, the additive-reinforcement theory is falsified and the fix is the split-call architecture (**TODO #44**): two parallel API calls via `UrlFetchApp.fetchAll` — Call 1 = isolated RCV-only prompt (free of this prompt's Dwelling/structures framing), Call 2 = existing prompt, merge Call 1's `rcv_amount` over Call 2's. Awaiting smoke test.
  - **@27 (2026-06-04) — @26 failed AND destabilized; full structural-alignment port.** @26 produced variance ($22,563 / $22,623 / $25,328 / $25,563) where pre-@24 was deterministic $22,563 — i.e. D1+D2 *partially* worked (model now sometimes reaches Contents / Other Structures) but did not converge. @27 ports the remaining diagnostic gaps **on top of** D1+D2: **D3** = standalone `=== DEPRECIATION FORMAT ===` section ("sum ALL NRD found ANYWHERE — Dwelling, Other Structures, Cosmetic Hail, supplements") placed **before** RCV as the multi-section-scan rehearsal that primes RCV totalization; **D4** = final-invoice's explicit "search these sections" enumeration added to LINE ITEM EXTRACTION. Section order realigned to final-invoice: **ACV → PWI → DEPRECIATION → RCV**. All ports byte-faithful, adapted only for field naming (no `recoverable_depreciation` field, so that bullet dropped — parallel to @24's `total_acv` drop). D1 recency guard + D2 field order kept verbatim. PAGE 2 GUIDE SKIP deliberately NOT ported (would risk discarding real page-2 measurements on Roofr dual-path reports). **NRD/policy note:** D3 makes `non_recoverable_depreciation` a comprehensive multi-section sum; downstream (`index.html:885-887`) only feeds NRD into contractAmount/commission math when `is_acv_policy === true`. Kelsey Bonnell is an **RCV policy** (`is_acv_policy === false` — its "Less Depreciation (3,823.64)" is *recoverable*: parentheses + a "Total Recoverable Depreciation" line), so D3 has **zero** commission impact on the Kelsey smoke test; NRD shows informationally only. D3's NRD-on-ACV billing path is unvalidated by this test → **TODO #45** (validate on a historical ACV job; block ACV-job production use of job-cost-calc until verified). **This is the FOURTH and FINAL additive attempt.** If @27 fails: roll back to @23 tonight (`clasp deploy --deploymentId <prod> --description "ROLLBACK to @23 …"`) and do a clean-room prompt rewrite next session using final-invoice as the structural template — no further iterative patches on this prompt (**TODO #44**). Awaiting smoke test (primary target `rcv_amount` = $26,328.96; secondary target NRD vs @26 for regression).

## Archived from CLAUDE.md on 2026-06-17

final-invoice-calculator PWI grand-total anchoring post-mortem (@14 incomplete attempt → @15 fix), trimmed from the CLAUDE.md inventory entry. The extraction bug is RESOLVED (shipped @15, 2026-06-01); the CLAUDE.md entry retains a compact pointer here. UX follow-up tracked at TODO #42.

### final-invoice-calculator — PWI grand-total anchoring (fixed @15, 2026-06-01)

- **Bug history:** PWI extraction grand-total anchoring (@15, 2026-06-01) — `total_rcv` came back PWI-stripped (Bruce Powers AR: $24,575.35 vs AR truth $28,940.16 = base $24,575.35 + $4,364.81 PWI). Downstream calc (`index.html:1064-1067`) assumes `total_rcv` is FULL RCV including PWI, so the breakdown then double-deducted uncommitted PWI → underbill. **@14 was an incomplete first attempt** — misdiagnosed as a missing PWI summary-section label and added "Code Upgrade Paid When Incurred" to §5; this changed nothing because the actual failure was grand-total *anchoring*, not a missed label. The Bruce Powers AR prints RCV three ways: PWI-INCLUSIVE per-coverage headline summaries (page-1 "Summary For Coverage A" $27,871.67 + "Coverage B" $1,068.49 = $28,940.16) AND a prominent PWI-EXCLUDED recap "Total" ($24,575.35, page 8/9); the model latched onto the recap. **@15 is the actual fix:** §5 now (a) sums the per-coverage headline "Summary For Coverage X" RCVs (PWI already baked in — the "Paid When Incurred" memo column is not a deduction), (b) falls back to detail sub-summaries including any "Paid When Incurred" block, and (c) GUARDs against using any "Line Item Total"/"Total"/grand-total recap line. Mirrors job-cost-calculator's §5 anti-grand-total guards (the gold standard, which extracted this same AR correctly). UX follow-up: TODO #42.

---

## Archived from root CHANGELOG.md on 2026-05-13

### 2026-04-30 — First Claude Code session, blog deploy refactor, AI visibility audit

- **First Claude Code session.** Hank's first working session in Claude Code (CLI) rather than claude.ai chat — full repo access, no copy-paste friction.
- **Blog automation refactored to deploy via GitHub instead of Netlify.** Replaced `deployToNetlify()` with `deployToGitHub()` in `blog-automation/BlogAutomation.js`; posts now ship via the GitHub Contents API into `website/blog/` and Netlify auto-deploys from the resulting commit. Eliminates the prior zip-deploy path that could overwrite files not present in the bundle.
- **Added `recoverPostFromStored()`** in `BlogAutomation.js` — rebuilds a published post's HTML from data persisted in Script Properties and re-pushes via GitHub. Used when a deploy wipes a post that wasn't in the source tree.
- **Recovered hailstorm post.** "What to Do Immediately After a Hailstorm Damages Your Roof" was wiped by a Netlify zip-deploy and recovered from stored data using the new helper (commits d131cda, 73eb1b1).
- **AI visibility audit completed.** Drafted a 30-day plan for improving discoverability in AI search/answer surfaces, plus a rewritten About page narrative (`docs/about-page-content.md`). About page rewrite applied to `website/about.html` in this session.
- **Site-wide roof / claim count standardization.** Replaced stale "thousands of..." and "1,000+ / 1K+" figures across `index.html`, `insurance-claims.html`, `roof-replacement.html`, and `blog/index.html` with the canonical 500+ roofs replaced and 500+ insurance claims navigated (commit e2ec89e). Aligns the full public site with CLAUDE.md and the new About page.

### 2026-04-29 — Repository foundation

- Initial commit: cloned all 10 Apps Script projects via clasp
  - `ai-lead-helper`, `blog-automation`, `evidence-package`, `final-invoice-calculator`, `job-cost-calculator`, `job-submittal`, `lead-capture`, `paid-appointment-tracker`, `review-response-drafter`, `supplement-agent`
- Added `CLAUDE.md` (architecture / operating manual for AI assistants)
- Added `README.md` (human-facing overview)
- Added `CHANGELOG.md` (this file)
- Set up GitHub repo at `Halford-cia/acacia-roofing-systems` (private)

### Pre-version-control (pre-2026-04-29)

Highlights from sessions prior to repo creation:
- **2026-04-24** — Built Lead Uploader (bulk paste-and-assign tool); SEO fixes for blog post titles; sales presentation deliverable
- **2026-04-21** — Website final zip review; Netlify deployment cycle
- **2026-04-13** — Evidence Package Builder shipped (4-step web form, photo compression, PDF + email)
- **2026-03-30** — Final Invoice Calculator overhaul (multi-section RCV bug fix, Shared Drive folder, manual override, Melio link); Acacia Brain trained on Reps DB, shingle pricing, company policies; Document AI added to all three calculators with graceful fallback
- **2026-03-25** — Marketing strategy session (Google LSAs, Performance Max, Meta Advantage+, AdAmigo)
- **2026-03-23** — Final Invoice Calculator session: extraction prompt overhaul, ROOF/NON-ROOF categorization, payment instructions
- **2026-03-03** — Material Order sidebar fixes; supplier email updates (Cameron Ashley, ABC Supply, QXO)
