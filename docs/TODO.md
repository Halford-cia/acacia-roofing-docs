# Acacia Roofing Systems — Active TODO

Live TODO list and Done log. Read this alongside CLAUDE.md at the start of every session.

For architecture, conventions, project inventory, and Manual Action Items, see [CLAUDE.md](../CLAUDE.md).

For historical archived content (pre-May Done items, Stage A/B/C detail, bug-fix history), see [STAGE_HISTORY.md](STAGE_HISTORY.md).

---

> **Numbering note (2026-05-11 grooming):** TODO numbers are preserved across grooming sessions to keep historical references (commit messages, memory entries) valid. Physical ordering reflects current priority — non-monotonic numbering is intentional. Items removed or moved to Done leave gaps.

> **Priority order (2026-05-21 grooming):** Numbers preserved per convention; reading top-to-bottom = priority high-to-low.

44. **CONTINGENT — clean-room rewrite of job-cost-calculator's extraction prompt.** Triggers ONLY if @27 (full structural-alignment port — D3 DEPRECIATION FORMAT + D4 section-search list + ACV→PWI→DEPRECIATION→RCV reorder, deployed 2026-06-04) fails the Kelsey Bonnell smoke test. Background: the Kelsey Bonnell AR (USAA/ALLCAT, RCV policy) fails RCV extraction in job-cost-calc (`rcv_amount` = $22,563.05 = Dwelling + Code Upgrade PWI only; drops Contents $59.54 + Other Structures $3,706.37; correct = $26,328.96) while final-invoice-calc extracts it 15/15 with the **identical** §5 directive. **FOUR bounded additive prompt fixes were tried and exhausted:** @24 (port final-invoice §5 architecture), @25 (concrete example seed), @26 (D1 recency guard + D2 field reorder — partially worked but *destabilized* into multi-value variance), @27 (D3+D4 full scaffolding port). If @27 fails, the additive-reinforcement theory is fully falsified → the surrounding prompt context (MULTI-STRUCTURE/structures[] framing, measurement-heavy output) structurally defeats §5 and can't be removed (job-cost needs structures[] for Roofr reports). **Plan B (next session, NOT tonight):** clean-room rewrite of the prompt using final-invoice-calc as the structural template — no further iterative patches on the current prompt. The split-call architecture (two parallel API calls via `UrlFetchApp.fetchAll`: Call 1 = isolated RCV-only prompt free of Dwelling/structures framing, Call 2 = existing prompt, merge Call 1's `rcv_amount`; feature flag `USE_SPLIT_RCV_EXTRACTION`; full audit + Design A/B captured 2026-06-04) is **one candidate approach within** the clean-room rewrite, not the automatic next step — evaluate it against a straight rewrite first. **Tonight's Plan B execution if @27 fails:** `clasp deploy --deploymentId AKfycby_GZW_xdKlLTzOfM0gLn6KwpuhVf25RadEzwQWcridovMUfzx4r9uCpllt3JLOyfth1w --description "ROLLBACK to @23 — @24-@27 prompt iterations failed; clean-room rewrite next session"`, confirm live, update CLAUDE.md to rollback state, end session.
45. **Validate @27 D3 NRD multi-section sum behavior on ACV-policy jobs.** D3 (shipped @27, 2026-06-04) changes `non_recoverable_depreciation` extraction to be comprehensive across Dwelling + Other Structures + Cosmetic Hail + supplements. On **RCV policies** (e.g. Kelsey Bonnell) this has NO commission impact — NRD is informational only. On **ACV policies** (`is_acv_policy === true`) NRD feeds `contractAmount` math at `index.html:885-887` (`depreciationNotPaid = NRD − customerPayingDepreciation; contractAmount = adjustedContract − depreciationNotPaid`), which flows into officeFee/profit/repCommission — so a more-complete NRD changes commission output (direction: more accurate, typically lower contractAmount). Work: find one historical ACV-policy job in Master Tracker, run its AR through job-cost-calc @27, verify NRD extraction matches expected vs prior partial-sum behavior, and confirm the commission math output is sensible. **Block ACV-job production usage of job-cost-calc @27 until this is validated.** Discovered during the @27 NRD-usage audit, 2026-06-04.

15. **Repair / non-roof job submittal flow.** Sibling intake to the main job-submittal rep form, dedicated to ancillary work — fencing, gutters, siding, and similar non-roofing scope. Integrates with the existing Acacia crew calendar for scheduling all non-roof jobs. Captures relevant fields per work type (likely different from roof's full adjuster-report flow — no extraction step, no shingle/PWI/ACV logic, but still customer/address/install-date/crew-assignment/contract-amount). Open design questions: separate web app vs new branch of existing rep form? Master Tracker columns (re-use existing schema or new tab)? Material order routing (non-roof suppliers differ)? Commission structure (does Acacia Share / Rep Share apply identically)? Priority #2-3 per 2026-05-21 grooming — meaningful operational gap; today every non-roof job is managed ad-hoc outside the system. Spec locked 2026-05-26 — see docs/ANCILLARY_SUBMITTAL_SPEC.md. Phase 1 (gutters) complete 2026-05-27 — see ancillary-submittal/. Phase 2 (fencing, siding, painting, drywall, other) complete 2026-05-27. Phase 3+ (My Ancillary Jobs view, Brain accessors, production cutover) pending.
39. **Extend Commission Builder (acacia-portal) to read Ancillary Jobs tab. HIGH priority.** Deferred from Phase 1 wrap-up 2026-05-27. Ancillary jobs are standalone commission projects — Phase 1 dropped the Rep Share / Acacia Share columns and the Rep Job Costs writes from `ancillary-submittal` once we recognized that ancillary work is structurally different from per-incident rep costs. Currently ancillary commissions are computed downstream via QBO data only; no automated Commission Builder integration. Work: extend `acacia-portal/CommissionGeneratorService.js` (or sibling) to enumerate Ancillary Jobs rows in the same commission cycle as roof jobs, apply Tier 1/2/3 office-fee math against `Contract Amount` (col 18), and surface them on the commission spreadsheet alongside roof jobs. Also: wire up `onAncillaryStatusEdit` responsibility #3 (currently a logged no-op slot at `ancillary-submittal/Code.js`) to fire whatever Commission Builder hook makes sense. Spec §8 was rewritten 2026-05-27 to reflect this redesign.
40. **Ancillary "Other" trade events do not trigger calendar date blocking.** `ancillary-submittal/Constants.js:ANCILLARY_BLOCKING_KEYWORDS` is the fixed list `[GUTTERS, FENCE, SIDING, PAINTING, DRYWALL]`. When a rep submits with trade = `other`, the event title uses the rep-typed trade name (e.g. `Window Replacement @ 123 Smith St`) — matches none of the keywords, so subsequent ancillary submissions won't see "Other" events as date blockers (violates spec §7 "everything blocks everything"). Low-volume gap from Phase 2 wrap-up 2026-05-27; dispatch can manually insert `UNAVAILABLE` events for real conflicts. Fix path: prefix "Other" trade event titles with a stable token (e.g. `[ANC]`) and add it to `ANCILLARY_BLOCKING_KEYWORDS`. Defer until volume increases.
32. **Brain lead-cost hardcoded values — fix architectural drift. HIGH priority (promoted 2026-05-11 grooming).** `job-submittal/Code.js:4894` in `getRepJobCosts` hardcodes `amount: 75.75` as the rep share when reading from Leads Tracker as a fallback source (when a lead exists in LEADS TRACKER but isn't yet in Rep Job Costs). This static value:
    a. Doesn't differentiate Email leads ($75.75 rep share post-50/50 split of $151.50) from Text leads ($75 rep share post-50/50 split of $150) — after the text price fix (Change B, 2026-05-11), this static $75.75 silently overstates text-lead costs by $0.75.
    b. Won't track if pricing changes in the future. Constants in `ai-lead-helper/LeadsAutomation.js` (`LEAD_COST_EMAIL`, `LEAD_COST_TEXT_NEW`, `REP_SHARE`) are the source of truth — Brain shouldn't be independently making up amounts.

    Fix options:
    - Read lead source from the LEADS TRACKER row (Lead Source column) and apply the right constant (more correct, more code)
    - Stop reporting fallback amounts entirely when a lead is in Leads Tracker but not Rep Job Costs — return "Cost calculation pending" instead (simpler, more honest)
    - Centralize cost-per-lead math in a shared helper that both ai-lead-helper and Brain consume (best architecture, biggest refactor)

    Discovered during the 2026-05-11 Brain audit alongside the Reversed-filter bug. Magnitude of error per text lead is small ($0.75) but compounds across every rep balance query — promoted to high priority during 2026-05-11 grooming.
31. **Paid Appointment process improvements (workstream). HIGH priority (promoted 2026-05-11 grooming).** Multiple known issues bundled for a future focused session:
    a. paid-appointment-tracker/Code.js:392-398 writes 10 values into 16-column Rep Job Costs sheet — schema mismatch puts string data into numeric Acacia Share column, drops Status to wrong column, leaves Lead Reference blank. Currently masked by manual re-runs of `cleanupRepJobCostsSchema` and `backfillLeadReferences` in job-submittal/Code.js (at :4321 and :4486 respectively, no triggers). Fix at the writer, not the cleaner.
    b. Same selection-based UX pattern shipped in bad-lead-app should apply to paid-appointment reporting — let reps select which appointment they performed from an authoritative list rather than typing address.
    c. Auto-paid-out trigger on commission close — when a commission is paid for a rep, automatically flip all "Pending — deduct at job closing" rows for matching customer/address to "Paid Out" status. Eliminates manual cross-referencing at commission time.

    Investigation needed before scoping (a) and (c); design pattern from (b) ready to apply now that bad-lead-app has shipped (Phase 3 + Phase 5, 2026-05-12).
36. **PWI dollar math in job-submittal Master Tracker writes — Pass 4 of TODO #33.** Currently PWI YES/NO decisions in the rep form (`Form.html displayPwiDecisions`) are cosmetic: they're captured in `pwiDecisions` state, sent to server as `formData.pwiDecisions`, and recorded as text in `sendRepNotificationEmail`'s body (Code.js:1543). **No dollar adjustment is applied to Master Tracker col 15 (Contract Amount) or col 18 (Invoice Amount).** Both columns use `data.rcv_amount` as-extracted, which includes the full PWI line item RCV regardless of whether the rep committed to performing those items. Result: if a rep marks 4 of 6 PWI items as NO ($1,200), the tracker still reports the full $30,000 RCV — overstating expected revenue by $1,200 that insurance won't release. Scope decisions for Pass 4: (a) minimal server-only fix — subtract NO'd PWI dollars from `fullContractAmount` and `invoiceAmount` before writing; (b) full UX with client-side running totals matching `final-invoice-calculator`'s Model A (baseline = `total_rcv − paid_when_incurred_total`, YES = ADD RCV back); (c) hybrid — server math now, UX polish later. Reference: diagnostic in 2026-05-14 session (post-Pass-3 verification). Medium priority — overstated tracker doesn't break anything immediately but causes office reconciliation friction when insurance payouts don't match expectations.
5. **Auto-reverse Rep Job Cost on manual onLeadStatusEdit Good→Bad edit.** Form path shipped 2026-05-11 (polling trigger picks up Bad Lead Reports submissions, calls reverseRepJobCostByLeadRef_). Rep web app path shipped 2026-05-21 (synchronous reversal via reverseRepJobCostByCustomerAddress_ for non-Email leads). Admin web app path shipped 2026-05-19 (TODO #35 OP3 reversal). Remaining gap: when office manually types 'Bad' directly in LEADS TRACKER Status column, onLeadStatusEdit:1137 only sends a credit email — no reversal. Add reverseRepJobCostByLeadRef_ call in the Good→Bad branch. Procedural workaround in place (use admin web app instead of manual edit, per Kelcie heads-up email 2026-05-21), but underlying landmine remains.
4. **Remove ADMIN_CC from per-lead rep notifications and consolidate exception emails further.** Already partially scoped — see 2026-05-01 session for the high-leverage cut to per-lead `cc:` on `sendLeadEmails` and `onNewLeadSubmitted`. Remaining work: review whether other per-lead admin sends can be consolidated into the existing `sendBadLeadSummaryEmail_` exception channel; audit `redistributeLead` CC behavior; quantify after-cut quota usage from execution logs.
34. **Auto-review-request text after job close.** Workflow: when a job is marked complete / Paid in Full, automatically send the customer a text message requesting a Google review. Promoted from Backburner to standard TODO during 2026-05-11 grooming. Low-medium priority. Design questions: trigger source (Master Tracker status edit? Commission close?), timing offset (immediate? 3 days later?), message content + branding, opt-out handling, integration with existing texting infrastructure.
17. **Brain write to Rep Job Costs sheet — unblocked.** Originally blocked pending portal architecture decisions; portal Stage A/B/C now complete, so this can proceed. Brain currently READS Rep Job Costs (via `getRepJobCosts` in job-submittal/Code.js) but doesn't write back. Use case: rep asks Brain a question that should create a cost entry (e.g., "log a $50 chargeback"). Recommended path: have Brain call into `CostsService.addCost` (the schema-defensive writer shipped in Stage A/B) rather than writing directly. This avoids Brain duplicating the schema knowledge and inherits future schema changes automatically.
22. **Commission Paid status trigger → Reps Cumulative Commissions auto-update.** Deferred from Stage B Commit 4 design. Workflow: when Master Tracker job Status flips to "Commission Paid" (a future status, currently informal), a trigger should auto-increment the rep's Reps sheet `Cumulative Commissions` by this commission's Salesman Commission amount. Reasoning: Cumulative Commissions should reflect actually-paid commissions, not generated-but-unpaid. Office adjusts manually until this lands.
3. **Auto-detect lead source from vendor email format (uploader enhancement).** Bulk uploader source toggle (Email vs Text) shipped 2026-05-11 — see Done section, Change C. Remaining want: auto-detect source from the vendor email's content/sender pattern so rep doesn't have to remember to toggle the radio. Text leads come from a different vendor than email leads; sender domain + subject patterns likely identifiable. Low-medium priority — manual toggle works fine, this just saves a click and prevents "oops forgot to switch" errors.
38. **Live smoke tests pending — three features deployed this week awaiting real-world verification:**
    a. Greg/claim-type @73 — first real PA or Appraisal adjuster-report submission exercises subject prefix + body header
    b. Vendor-routing @4 — first real text-lead bad submission exercises auto-create Lead Source column + Processed pre-stamp logic + rep-path reversal
    c. Lead clock shift @12 — first real lead with parser-extracted date exercises bulk uploader UI + col F write + checkLeadAging auto-promotion on new anchor

    Track until each is verified.
29. **Public Adjuster (PA) handling bucket.** Claim Type column on Master Job Tracker shipped 2026-05-21 (col 41, persists formData.claimType per TODO #29 partial). Remaining: separate KNOWN_PUBLIC_ADJUSTER_VENDORS list in Constants.js, new PA-distinct bucket on getJobCostsByAddress, Panel B field decision on whether PA cost affects commission Gross Profit, pass-through fee categorization (Acacia cost vs homeowner cost). Alex Rippee (truescopeadjusting.com) bills currently land in 'uncategorized' where office handles manually until this lands. LOW priority.
20. **Stage 5 follow-up — Cost-type split logic.** `CostsService.addCost` defaults to `repShare = amount`, `acaciaShare = 0` (rep-pays-100%). Roof Inspection callers must explicitly pass 50/50 split via `{ repShare: amount/2, acaciaShare: amount/2 }`. May need a `DEFAULT_SPLIT_BY_TYPE` map in `Constants.js` once production usage shows the per-type defaults that would prevent caller-side errors. Memory: `project_addcost_split_defaults.md`.
7. Ridge upgrade rep-facing warning in form (backend done) — LOW priority.
8. More shingle colors/types (dynamic dropdown per shingle type) — LOW priority.
23. **External link hygiene — `rel="noopener noreferrer"`.** Portal `target="_blank"` links (Panel D file links, post-generate banner button) lack `rel="noopener noreferrer"`. Standard hygiene; sandbox likely mitigates the practical attack surface, but defer to a polish sweep. See memory entry `project_external_link_hygiene.md`.
24. **Stage B polish — Processed banner visual prominence.** Current Processed banner (`.cb-readonly-banner`) is amber-toned and easy to miss when office returns to a Processed request. Consider greener + larger styling so it reads "this is locked, view-only" at a glance. Per session feedback during Stage B end-to-end verification.
25. **`debugQboState` — fix accessTokenExpiresAt + refreshTokenExpiresAt + scope readouts.** Stage C Commit 1 verification showed these three fields rendered missing/null — the OAuth2 library v43 stored-token JSON shape differs from the assumed `granted_time` / `expires_in` / `x_refresh_token_expires_in` / `scope` keys. Cosmetic polish only (`hasAccess`, `companyId`, `environment` are correct, which is what matters); fix when next adjacent QBO work touches `debugQboState`.
27. **Stage C Commit 4.5 — UI polish (deferred).** Candidates: refresh-success toast/feedback (vs the current silent re-render), view-pane loading overlay during the 1–2 sec QBO round-trip, prominence sweep on the QBO Income mismatch warning. Deliberately deferred until office uses the system on real production data — speculating on polish needs before real usage is premature. Pick up when office surfaces specific friction. **Status questioned during 2026-05-11 grooming — confirmed still pending (none of the three candidates shipped); deferred per the "office surfaces friction" criterion.**
13. **Xactimate bid generation via Acacia Brain.** Goal: ask Brain to produce an Xactimate bid for a job based on extracted adjuster data, return either an Xactimate-importable file or a line-item-by-line-item structured bid. Backburner — interesting capability, not immediate priority.
16. Cameron Ashley CompanyCam texts
41. **Bump actions/checkout from v4 to current (Node.js 24-compatible).** The docs-mirror workflow run (2026-05-28, run #5) logged a deprecation warning — `actions/checkout@v4` runs on Node.js 20, which GitHub is deprecating. Update the `uses:` pin in `.github/workflows/sync-docs.yml` to the current checkout major. LOW priority — workflow runs fine today; cosmetic until GitHub forces the runtime cutover.
42. **Final-invoice-calculator PWI UX — refactor to mirror job-cost-calc's dedicated PWI section.** Currently PWI items are intermixed with regular ROOF/NON-ROOF line items in final-invoice-calculator's checkbox list (index.html). Job-cost-calculator presents PWI in a dedicated section with YES/NO buttons and an orange callout — clear visual separation between WNP decisions ("check if NOT performed") and PWI decisions ("YES/NO will this be billed"). Conflating these in the same UI primitive is a UX failure mode adjacent to the 2026-06-01 PWI extraction drift bug. Work: extract PWI items into their own rendered block in final-invoice-calc, YES/NO buttons replacing checkboxes; update server-side response shape if needed (separate `pwi_items[]` array); update Invoice Breakdown calc + PDF generation to read from new state shape. Visual parity target: job-cost-calculator. MEDIUM priority — current UI works post-extraction-fix but rep clarity would improve and visual separation prevents future bugs that conflate WNP and PWI math.

**Done:**
- ✅ Stage B complete (commits 1, 2, 3, 3.5, 4, 5, 5.5, 6) — 2026-05-09 (see docs/STAGE_HISTORY.md for details)
- ✅ TODO #6 — Rep Job Cost coverage workflow — 2026-05-09. Shipped via Stage B. See STAGE_HISTORY.md for detail.
- ✅ TODO #21 — Rep My Jobs view (verify on first rep login) — 2026-05-09. Verified.
- ✅ Stage C complete (commits 0–5 + fix-up, Intuit prod app live) — 2026-05-11 (see docs/STAGE_HISTORY.md for details)
- ✅ Lead lifecycle fixes (Changes A + B + C, commit c6d0b98, ai-lead-helper @4) — 2026-05-11. `onLeadStatusEdit` manual-Bad reverse, `LEAD_COST_TEXT_NEW` $150 correction, bulk uploader Email/Text source toggle.
- ✅ Bad Lead Report web app v1 (TODO #30, commit bfbbea6, ai-lead-helper @4) — 2026-05-11. Replaces Google Form with rep-scoped selection, strict @acaciaroofs.com auth, required photo upload, synchronous reversal, USER_DEPLOYING + DOMAIN deployment topology.
- ✅ Reversed-prefix filter hardening (commit 6a0b1da, job-submittal @70 + acacia-portal @17) — 2026-05-11. Brain `getRepJobCosts` and `CostsService._isUncovered` both use prefix-form status filter; future-proofs against new Reversed variants.
- ✅ acacia-portal Stage C Commit 5 fix-up (commit ef117de, acacia-portal @16) — 2026-05-11. Purchase entity uses `EntityRef.name` (not `VendorRef.name`); Juggernaut loose substring; supplies account pattern; Alex Rippee removed from supplements.
- ✅ ai-lead-helper manifest formalized (commit 6f77679) — 2026-05-11. USER_DEPLOYING + DOMAIN access, explicit oauthScopes (resolves "Sorry, unable to open the file" first-load auth errors).
- ✅ Tier 1 audit fixes (commit d5ffb16, ai-lead-helper @8) — 2026-05-12. DST timezone bug at onNewLeadSubmitted + redistributeLead, LockService on checkBadLeadFormSubmissions + submitBadLeadReport, setFormLocked client UX, CLAUDE_MODEL → claude-opus-4-7, removed dead createBadLeadForm function.
- ✅ bad-lead-app split (commit 5850c2e, bad-lead-app @1 + ai-lead-helper @9) — 2026-05-12. Split Bad Lead Report web app into standalone project with USER_DEPLOYING + DOMAIN topology to solve permissions architecture problem (reps can't have Master Tracker write access). 5 functions moved (deleted from ai-lead-helper), 3 helpers duplicated with ⚠️ DUPLICATED markers, 668-line BadLead-Index.html removed.
- ✅ bad-lead-app v2 + ai-lead-helper polling trigger relax (commit 16e55c4, bad-lead-app @2 + ai-lead-helper @10) — 2026-05-12. Fixed v1 bug where getOutstandingLeadsForRep read from Rep Job Costs (empty for Pending leads). Now reads LEADS TRACKER, synchronous Status flip + defensive row verification, polling trigger queues credit email when Status already Bad. syncBadLeadFormReps retired (Google Form path dead).
- ✅ BAD_LEAD_FORM_URL bump (commit 30e6a01, ai-lead-helper @11) — 2026-05-12. Constant updated to point at bad-lead-app @2.
- ✅ TODO #35 — Admin bad-lead-app entry point (bad-lead-app @3) — 2026-05-19. New admin view at the same URL gates allowlist (joshua/kelcie/henry/office) into a separate flow for leads >48hr. Reports flip LEADS TRACKER Status via script write (no onLeadStatusEdit immediate Tony email), queue Tony credit via Bad Lead Reports tab (batch path), reverse Rep Job Costs row by Rep Email + Customer + Address with Reversed (Office Override) status, and write a REVERSE Brain Audit row. Per-admin window override map (henry@ = 45 days, default 14 days). Already-covered cost rows refuse OP3 with traceable office notice including the covering job address.
- ✅ Required claim-type dropdown + Greg supplement email branching (job-submittal @73) — 2026-05-20. Required claim-type dropdown (Standard/PA/Appraisal) added to rep form after adjuster-report extraction. PA and Appraisal selections prepend clarifying headers to Greg's supplement email so Juggernaut knows to skip full supplementing and only handle final invoicing. HTML5 required + server-side validation defense-in-depth. Master Tracker column for claim-type tracking (would partially satisfy TODO #29) deliberately skipped this commit — clean follow-up.
- ✅ Vendor-aware bad-lead routing (bad-lead-app @4) — 2026-05-21. New Lead Source column added to Bad Lead Reports tab. Both rep and admin submit paths now write source to that column and pre-stamp Processed='Yes' for all non-Email sources, so the polling trigger never queues Tony credit for text leads (or any future non-Email vendor). Email leads continue current behavior (blank Processed → polling queues Tony). Rep-path Good→Bad text-lead Rep Job Cost reversal now fires synchronously via reverseRepJobCostByCustomerAddress_, closing the previously manual-only gap for text leads. Helper refactored to accept status string as parameter (admin uses 'Reversed (Office Override)', rep uses 'Reversed (Bad Lead)').
- ✅ TODO #29 partial close-out — Claim Type column on Master Job Tracker (job-submittal @74) — 2026-05-21. Master Job Tracker now persists formData.claimType at column 41 (auto-created on first submit; mirrors bad-lead-app Lead Source pattern). Cash bids write blank; insurance writes the validated enum from yesterday's 325055f dropdown work. Schema source in createMasterJobTrackerSilent also updated, with WNP Totals/WNP ACV Total string drift fixed in the same touch. Broader PA-handling-bucket work (separate vendor list, GP treatment) deferred.
- ✅ Lead date clock shift — Pending→Good anchors on Date of Lead (ai-lead-helper @12) — 2026-05-21. The 48-hour auto-promotion clock now uses Date of Lead (col F) instead of Email Sent At (col J), matching the actual visit-day workflow rather than the upload-time workflow. Bulk uploader's parser output (`apptTime` MM/DD H AM string) now flows to col F via a new server-side helper resolveLeadDate that does nearest-occurrence year resolution. Uploads hard-block if any lead's date is missing or unparseable (server-side validation + client-side editable Time field with inline year preview + disabled upload buttons). Display deadlines in sendLeadEmails / onNewLeadSubmitted / redistributeLead shifted to match new anchor. Fallback to Email Sent At when Date of Lead blank/invalid (edge case, office always sets per Hank's workflow). Redistribution semantics unchanged — keeps original Date of Lead, new rep gets the remainder of the original clock. Also converts ai-lead-helper to in-place deploy pattern (matches bad-lead-app + job-submittal convention) and fixes pre-existing @10/@11 URL drift in CLAUDE.md inventory.
- ✅ TODO #37 — Tony bad-lead credit email format redesign + bad-lead-app reason dropdown (commit 8b8481d, ai-lead-helper @13 + bad-lead-app @5) — 2026-05-23. Shipped vernacular reframe ('Acacia - Bad Lead Report' subject; 'Here are the bad leads:' greeting), stripped Sales Rep + Lead Source from per-lead block, added accounting@topconstructionleads.com as 3rd recipient, applied identical format to both batch and immediate send paths so admin (>48hr) and rep (<48hr) reports look identical to Tony, added clearBadLeadQueue utility, replaced 6-option reason dropdown with 8 new options via shared BAD_LEAD_REASONS const. Trigger re-enabled 2026-05-23 evening; clear queue dumped 34 stale entries (duplicates of Hank's manual 2026-05-23 AM email to Tony).
- ✅ TODO #14 — Supplement upload workflow adoption (commit 8b554d2, job-submittal @75) — 2026-05-25. Activated the previously-implemented-but-never-used supplement workflow in Acacia Brain (📎 button → upload supplemented adjuster PDF → match to existing job → review new line items + PWI/WNP decisions → update Master Tracker contract/invoice + audit-line in Job Notes + office email). Phase A scope: extraction-prompt rewrite with document-type validation (rejects non-adjuster PDFs up front), explicit Supplement header promoted to signal #1, three-tier confidence self-rating (high/medium/low), case-insensitive NRD matching (capital-R and lowercase-r variants), Cosmetic Hail multi-section RCV summation, PWI scoping to supplement items only (original-scope drip edge / code upgrades stay excluded); schema add document_type + rejection_reason + confidence, drop unused new_items_detected; banner replaced with confidence-driven green/yellow/red tiers + item count (replaces brittle newItems.length vs allItems.length math that mis-fired yellow on successful extractions); fixed silent-skip schema-drift bug — `headers.indexOf('WNP ACV Total')` → `'WNP Totals'` at 2 locations (matchJobByNameAndAddress + submitSupplementDecisions). Live-tested 2026-05-25 against 3 baseline PDFs: Hoipkemeier (USAA/Allcat), Fitzsimons (USAA/Allcat), Alexander (Travelers/HOVER) — all extractions clean, banner colors correctly reflect signal strength per rubric. Phase B (portal migration) deferred — Kelcie has Brain access, no friction surfaced.
- ✅ TODO #33 — Job cost calculator extraction port to gold standard (commit 96b752e, job-cost-calculator @23) — 2026-05-25. Closes the multi-pass extraction-prompt audit (Passes 1, 1.5, 2, 3, 3.5 previously shipped to final-invoice-calculator @13 and job-submittal @72; Phase C now ports the four gold-standard elements to the standalone job-cost-calculator project): document-type validation (rejects non-adjuster/non-Roofr uploads up front via existing handleExtractionError client path), case-insensitive NRD matching (closes the 2026-05-11 Homeowners of America carrier bug — both capital-R and lowercase-r variants now recognized), Cosmetic Hail / multi-Summary-section RCV summation (Dwelling + Other Structures + Cosmetic Hail + Elevations + Interior + Code Upgrade PWI), three-tier confidence self-rating rubric. Schema additions (document_type, rejection_reason, confidence) layered additively on top of the existing measurement-fidelity schema — structures[], line_items[], squares-by-pitch, LF totals, solar detection, PWI all preserved verbatim, no fields removed. UI: new three-tier .banner-confidence-{low,med,high} CSS variants + #confidence-banner DOM element + handler in handleExtractionSuccess driven by `data.confidence`; hideBanners() extended to reset on retry. Reuses existing handleExtractionError red-status-banner path for doc-type rejections (no new error UI needed). Deploy hygiene: job-cost-calculator brought onto in-place clasp deploy pattern for the first time — canonical deploymentId `AKfycby_GZW_xdKlLTzOfM0gLn6KwpuhVf25RadEzwQWcridovMUfzx4r9uCpllt3JLOyfth1w` captured (gitignored .clasp.json local copy + CLAUDE.md inventory backfilled to parity with final-invoice-calculator / bad-lead-app format, commit 7e0219b). Live-tested 2026-05-25 against three adjuster reports (Hoipkemeier USAA/Allcat, Fitzsimons USAA/Allcat, Alexander Travelers/HOVER) — measurement-fidelity extraction preserved, confidence banner fires per rubric, no regressions vs prior behavior. Pass 4 (PWI dollar math in job-submittal Master Tracker writes) continues as TODO #36.
